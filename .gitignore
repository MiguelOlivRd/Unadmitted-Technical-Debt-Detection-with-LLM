# Correct folder formatting (no leading dot-slash, use trailing slash)
to_delete/
experiment_pipeline/checkpoints/
experiment_pipeline/data/

# Correct specific file formatting
experiment_pipeline/llm_inference_results.json

# Correct wildcard global folder match
**/__pycache__/